# Sifreli cikarim koprusu CI'i icin en dar, kanitla uyumlu ortam.
#
# Bu test `concrete.ml`/PyTorch, scikit-learn veya FastAPI kullanmaz; yalnizca
# Concrete'in `fhe` ve `tfhers` API'leriyle Rust tarayici sifreleyicisi arasindaki
# anahtar/ciphertext sozlesmesini denetler. `concrete-ml` eklenirse GPU PyTorch
# tekerlekleri (`nvidia-*`, `triton`) de indirilir ve GitHub runner diski dolar.
# Uygulamanin tam ML ortami `requirements.txt` olarak kalir.

concrete-python==2.10.0
numpy==1.26.4
pytest==8.3.5
